{
	"name": "boilerplate-backend",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "bun run gen && wrangler dev ./src/index.ts --live-reload --experimental-vectorize-bind-to-prod",
		"deploy": "wrangler deploy ./src/index.ts --minify",
		"test": "bun test --watch --coverage",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"gen": "bun gen:types && bun gen:list-rules-ai && bun gen:list-files-hidden",
		"gen:types": "wrangler types --env-interface CloudflareBindings",
		"gen:list-rules-ai": "bun run scripts/generate-list-rules-ai.ts",
		"gen:list-files-hidden": "bun run scripts/generate-list-files-hidden.ts"
	},
	"dependencies": {
		"defu": "^6.1.4",
		"hono": "^4.6.17",
		"nanoid": "^5.0.9"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250121.0",
		"@types/bun": "^1.1.17",
		"typescript": "^5.7.3",
		"vitest": "^3.0.2",
		"wrangler": "^3.103.2"
	}
}
