{
	"name": "@boil/sync-server",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"main": "./src/schema.ts",
	"scripts": {
		"dev": "zero-cache-dev -p src/schema.ts",
		"deploy": "fly deploy --remote-only",
		"test": "bun test --bail=1 --watch --coverage",
		"lint:fix": "biome check --fix .",
		"lint:fix-force": "biome check --write ."
	},
	"dependencies": {
		"@rocicorp/zero": "^0.12.2025012900"
	},
	"devDependencies": {
		"@boil/config-typescript": "workspace:*"
	}
}
