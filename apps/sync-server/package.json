{
	"name": "@boil/sync-server",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"scripts": {
		"dev": "zero-cache-dev -p src/index.ts",
		"deploy": "fly deploy .",
		"test": "bun test --bail=1 --watch --coverage",
		"lint:fix": "biome check --fix .",
		"lint:fix-force": "biome check --write ."
	},
	"dependencies": {
		"@rocicorp/zero": "^0.12.2025013001",
		"@boil/sync-zero": "workspace:*"
	},
	"devDependencies": {
		"@boil/config-typescript": "workspace:*"
	}
}
